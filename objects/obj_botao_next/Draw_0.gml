/// @description Desenhar botão na posição exata com escala reduzida
if (visible) {
    // Desenhar o sprite do botão com a escala reduzida
    draw_sprite_ext(sprite_index, 0, x, y, button_scale, button_scale, 0, c_white, 1);
}
