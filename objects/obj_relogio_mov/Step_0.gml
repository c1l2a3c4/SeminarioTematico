/// @description Movimento e troca de direção nas bordas, só se o jogo foi iniciado

// Verifica se o jogo foi iniciado
//if (global.jogo_iniciado) {
    // Movimento baseado na velocidade definida
  //  x += hsp;
  //  y += vsp;

    // Verifica se o objeto atingiu as bordas e inverte a direção
  //  if (x < 0 || x > room_width) {
     //   hsp = -hsp; // Inverte a direção horizontal
    //    x = clamp(x, 0, room_width); // Garante que o objeto não ultrapasse a borda
  //  }

 //   if (y < 0 || y > room_height) {
   //     vsp = -vsp; // Inverte a direção vertical
  //      y = clamp(y, 0, room_height); // Garante que o objeto não ultrapasse a borda
//    }
//}
